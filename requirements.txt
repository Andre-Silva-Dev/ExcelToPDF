# Dependências do ExcelToPDF
# Instale com: pip install -r requirements.txt

# Nota: o tkinter já vem incluído na instalação padrão do Python.
# Caso não esteja disponível no Linux, instale com:
#   sudo apt install python3-tk  (Ubuntu/Debian)
#   sudo dnf install python3-tkinter  (Fedora)

# Recomendado: usado para detectar e aplicar orientação automática (retrato/paisagem)
# na conversão via LibreOffice. Também necessário para arquivos .xlsx.
# pip install openpyxl
openpyxl

# Somente necessário para usar o Microsoft Excel no Windows:
# pywin32
